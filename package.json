{
  "name": "m1-watchdog",
  "version": "2.0.0",
  "description": "Minter Node watchdog with Telegram logging",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx eslint --ext .js ./src",
    "start": "node -r esm -r dotenv/config ./src/index.js"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^8.2.0",
    "esm": "^3.2.22",
    "minter-js-sdk": "^0.35.4"
  },
  "devDependencies": {
    "eslint": "^7.11.0"
  }
}
